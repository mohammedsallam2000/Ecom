import { Routes } from '@angular/router';
import path from 'path';
import { NavBarComponent } from './core/nav-bar/nav-bar.component';

export const routes: Routes = [{path: 'NavBarComponent', component: NavBarComponent}
];
